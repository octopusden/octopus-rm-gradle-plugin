jfrog-artifactory.version=4.24.4
jackson.version=2.12.3
httpclient.version=4.5.13
cyclonedx.version=1.7.4

# Validation-only: enables the internal dev Maven repo (init.gradle) so CRS
# branch snapshots resolve. Revert before merging to main.
use_dev_repository=dependencies
